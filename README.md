# Roomify for Accommodations - Platform.sh Quickstart

This is the platform.sh quickstart repository for RfA.

To update the RfA version, simply:

* take a snapshot
* edit project.make and update the version number
* commit and push
